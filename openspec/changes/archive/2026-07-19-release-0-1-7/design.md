## Context

Patch after 0.1.6 for Confluence-ready Mermaid export.

## Goals / Non-Goals

**Goals:** Changelog section, version bump, docs, tag v0.1.7, push.

**Non-Goals:** New features beyond tip.

## Decisions

1. Patch bump all workspace packages to 0.1.7.
2. Keep Запланировано under Unreleased (teams/, MCP HTTP, adaptive, full confluence page).
