## Context

Patch after 0.1.4 for consumer export (test + matrix).

## Goals / Non-Goals

**Goals:** Changelog section, version bump, docs, tag v0.1.5, push.

**Non-Goals:** New features beyond what is already on tip.

## Decisions

1. Patch bump all workspace packages to 0.1.5 (root, core, cli, mcp + lockfile).
2. Keep Запланировано under Unreleased (teams/, MCP HTTP, adaptive/mermaid).
