## 1. Core + CLI

- [x] 1.1 Core `exportMermaid` (graph from requirements + depends_on; coverage colors; markdown wrapper; `sdm.export.mermaid/v1`)
- [x] 1.2 Export public API; unit tests (nodes/edges/colors; missing level)
- [x] 1.3 CLI `export mermaid --role --level [--no-coverage] [--json]`

## 2. MCP + docs

- [x] 2.1 MCP tool `export_mermaid` + handler test
- [x] 2.2 CHANGELOG / README / AGENTS pointer
- [x] 2.3 `npm run verify` + playground smoke: `export mermaid --role java-developer --level middle --json`
