## Context

Patch after 0.1.3 for cert patch and MCP host wiring.

## Goals / Non-Goals

**Goals:** Changelog section, version bump, docs, local tag v0.1.4.

**Non-Goals:** New features beyond what is already on tip.

## Decisions

1. Patch bump all packages to 0.1.4.
2. Document `mcp config` / `mcp install` and `connect-mcp` as the agent path to wire Cursor.
